# ITSM 部署与更新指南

## 环境

| 组件 | 地址 | 说明 |
|------|------|------|
| K3s 集群 | 172.24.16.40 | 生产环境 |
| Harbor 镜像仓库 | harbor.szctdg.tech | 用户 `yangren`，代理缓存三个上游 |
| 代码仓库 | github.com/iostream911/itsm | 分支：main / uat / prd |
| 部署工具 | Helm | 版本管理和回滚 |
| 流量入口 | Ingress Controller (ingress-nginx) | 80/443 → 域名分流 |
| 证书管理 | cert-manager | 已安装，待配 HTTPS |

## 访问地址

| 服务 | 内网地址 |
|------|------|
| ITSM | `http://itsm.172.24.16.40.nip.io` |
| Zammad | `http://zammad.172.24.16.40.nip.io` |

> 公网端口映射后，将 nip.io 替换为真实域名即可。每个服务对应一个子域名（如 `itsm.szctdg.tech`），DNS 全部指向同一个公网 IP 的 443 端口，Ingress Controller 根据域名自动分发。

---

## 部署架构

```
用户浏览器
    ↓ https://itsm.szctdg.tech (公网)
WAF / NAT
    ↓ 内网 172.24.16.40:443
┌──────────────────────────────┐
│  Ingress Controller (nginx)  │  80/443 端口，根据 Host 头分发
│  ┌─────────────────────┐     │
│  │ itsm.xxx → itsm:3000│     │
│  │ zammad.xxx → zammad │     │
│  │            :3000    │     │
│  └─────────────────────┘     │
└──────────────────────────────┘
         ↓                  ↓
    ┌─────────┐       ┌─────────┐
    │  ITSM   │       │ Zammad  │
    │ Pod x 2 │       │  Pod    │
    └─────────┘       └─────────┘
```

镜像拉取链路：
```
K3s → Harbor 代理缓存 → SSH隧道 → Mac梯子 → 互联网
        ↑
    docker-hub-proxy / k8s-proxy / quay-proxy
```

---

## 更新流程

### 1. 改代码 + Build + Push

```bash
docker build -t harbor.szctdg.tech/itsm/itsm:v1.2.3 .
echo '@Yangren930924' | docker login harbor.szctdg.tech -u yangren --password-stdin
docker push harbor.szctdg.tech/itsm/itsm:v1.2.3
```

### 2. 部署

```bash
ssh huozhe@172.24.16.40
sudo helm upgrade itsm ./chart -n itsm \
  --set image.tag=v1.2.3 \
  --kubeconfig /etc/rancher/k3s/k3s.yaml
```

### 3. 提交代码

```bash
git add -A && git commit -m "v1.2.3: xxx" && git push origin main
```

---

## 回滚

```bash
# 查看历史
sudo helm history itsm -n itsm --kubeconfig /etc/rancher/k3s/k3s.yaml

# 回滚到上一版
sudo helm rollback itsm -n itsm --kubeconfig /etc/rancher/k3s/k3s.yaml

# 回滚到指定版本
sudo helm rollback itsm 1 -n itsm --kubeconfig /etc/rancher/k3s/k3s.yaml
```

---

## 修改配置

```bash
# 改副本数
sudo helm upgrade itsm ./chart -n itsm --set replicas=3 --kubeconfig /etc/rancher/k3s/k3s.yaml

# 改 Ingress 域名
sudo helm upgrade itsm ./chart -n itsm \
  --set ingress.host=itsm.szctdg.tech \
  --kubeconfig /etc/rancher/k3s/k3s.yaml
```

---

## 日常管理

```bash
ssh huozhe@172.24.16.40

# 查看 Pod
sudo kubectl -n itsm get pods

# 查看日志
sudo kubectl -n itsm logs -l app=itsm --tail=50

# 查看 Ingress
sudo kubectl get ingress -n itsm

# 查看 Helm
sudo helm list -n itsm --kubeconfig /etc/rancher/k3s/k3s.yaml
sudo helm history itsm -n itsm --kubeconfig /etc/rancher/k3s/k3s.yaml
```

---

## 集群组件一览

| 组件 | 命名空间 | 说明 |
|------|----------|------|
| ITSM | itsm | 核心服务，Helm 管理 |
| Zammad | itsm | 工单系统 |
| PostgreSQL / Redis / Memcached | itsm | Zammad 依赖 |
| ingress-nginx | ingress-nginx | Ingress Controller，监听 80/443 |
| cert-manager | cert-manager | HTTPS 证书自动管理 |
| Harbor | 172.24.10.8 | 镜像仓库 + 代理缓存 |
| ArgoCD | argocd | 已停用 |
